---
# TRADEMARK RULE: `title` and `description` become <title>, <meta name="description">
# and the Open Graph tags. The FedNow mark must never appear in either.
title: Pacsmith
description: An open-source ISO 20022 toolkit for US instant payments — build the send side, or evaluate what you would be adopting.
head:
  # Overrides Starlight's "Pacsmith | Pacsmith" on the home page. Mark-free, as
  # every <title> on this site must be.
  - tag: title
    content: Pacsmith — an open-source ISO 20022 toolkit for US instant payments
template: splash
hero:
  # The tagline is rendered in the page body only, never in metadata, so the
  # mark is used here the way it is allowed to be used: descriptively, visibly.
  tagline: An open-source ISO 20022 toolkit for the FedNow® Service. Apache-2.0, signed releases, no telemetry.
tableOfContents: false
---

Most of the 1,500+ institutions on the network can only receive. Building the **send**
side — ISO 20022 messaging, signing, timeout reconciliation, 24x7 operations — is where
the cost is. Pacsmith is a reference toolchain that lowers it: a message library, a local
simulator, a send gateway and client SDKs.

Two people arrive here wanting opposite first pages. Pick yours.

<div class="pacsmith-doors">
  <div class="pacsmith-door">
    ## Build

    <p>You are an engineer. You want it running before you want to read about it.</p>

    - [**Quick start**](/docs/quickstart/) — a payment that settles, one that is
      rejected, and the timeout case, in five minutes with Docker and curl.
    - [**Integration handbook**](/docs/handbook/) — the credit transfer flow,
      timeout reconciliation, returns, and getting through certification testing.
    - [**SDKs**](/docs/sdks/python/) — [Python](/docs/sdks/python/) (zero
      dependencies) and [Java](/docs/sdks/java/) (one), both integration-tested
      against a live stack in CI.
    - [**Gateway API**](/docs/gateway-api/) — idempotency-keyed submits, payment
      state, reconciliation and ops endpoints.
  </div>

  <div class="pacsmith-door">
    ## Evaluate

    <p>You are in risk, security or procurement. You want to know what you would be adopting.</p>

    - [**Signed releases**](/evaluate/#verifying-a-release) — keyless Sigstore
      signatures, with the exact `cosign verify-blob` command to check one yourself.
    - [**SBOM**](/evaluate/#software-bill-of-materials) — CycloneDX and SPDX, on
      every release.
    - [**OpenSSF Scorecard**](/evaluate/#openssf-scorecard) — a public, third-party
      score, updated weekly.
    - [**Vulnerability disclosure**](/evaluate/#reporting-a-vulnerability) — where
      to send a report and what happens next.
    - [**Licence**](/evaluate/licence/) — Apache-2.0, patent grant included.
  </div>
</div>

## Where the project stands

Early development, **v0.3.1**. The complete send loop works end to end — build, validate,
send, advise, reconcile — with production-style MQ semantics, the returns message set, and
Python and Java clients. It is **not production-ready**, pre-1.0 minor versions may break
APIs, and nothing here is certified by anyone. Message signing is still blocked on
access-controlled specifications. [The overview has the honest version](/docs/overview/).
